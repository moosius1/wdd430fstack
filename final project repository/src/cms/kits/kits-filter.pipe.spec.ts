import { KitsFilterPipe } from './kits-filter.pipe';

describe('KitsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new KitsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
